// Copyright (c) 2022, NIO Inc. All rights reserved.
//
// Any use, reproduction, distribution, and/or transfer of this file is strictly
// prohibited without the express written permission of the current copyright
// owner.
//
// Any licensed derivative work must retain this notice.
//
// knowledge.cpp : [file description]
//

#include <filesystem>

// A.B    则A为对象或者结构体；
// A->B


// 则A为指针，->是成员提取，A->B是提取A中的成员B，A只能是指向类、结构、联合的指针；
// A::B   是作用域运算符，A::B表示作用域A中的名称B，A可以是名字空间、类、结构；
// A：B   一般用来表示继承；

int main() {
  syslog(LOG_ERR, "Copy Camera Params Data Failed!");
  auto res = output.find("bsj");

  std::string output = "bsjdfbsjfb";
  std::filesystem::remove_all("/nio/tmp");
  std::filesystem::create_directories("/nio/tmp1");
  std::filesystem::exists("/nio/logs");

  short s = 1;
  static_cast<int> s;
  // 就是把short 的s转换成了int

  int var = (y < 10) ? 30 : 40);
  // ?
  // :

  // this 只能用在类内，通过
  // this访问类的所有成员，包括private、protected、public 属性的。 注意，this
  // 是一个指针，要用->来访问成员变量或成员函数。
}
